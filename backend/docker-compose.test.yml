# Minimal, ephemeral Postgres/Redis for integration/e2e tests in CI runners
# where Docker-in-Docker for testcontainers isn't available. Local
# development should prefer testcontainers (see tests/integration/conftest.py)
# since it needs no manual setup/teardown — this compose file is the fallback.
services:
  postgres-test:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ndma-cloud_test
      POSTGRES_PASSWORD: ndma-cloud_test
      POSTGRES_DB: ndma-cloud_test
    ports:
      - "55432:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ndma-cloud_test"]
      interval: 3s
      timeout: 3s
      retries: 10

  redis-test:
    image: redis:7-alpine
    ports:
      - "56379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 10
